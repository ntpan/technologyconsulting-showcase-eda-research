azure:
  eventHub:
    connectionString: "Endpoint=sb://tc-eda-iac-prod-customer-eventhub.servicebus.windows.net/;SharedAccessKeyName=pubsub;SharedAccessKey=nBbhXOl/J8CAQXEI2XVzB/TFnl1OSDZRIKOXCEMlM1Q=;EntityPath=customerchanged"
    consumerGroup: "$Default"

server:
  port: 9080

management:
  endpoints:
    web:
      base-path: "/"
    enabled-by-default: true
    jmx:
      exposure:
        exclude: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    info:
      enabled: true